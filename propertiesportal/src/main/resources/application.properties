spring.application.name=propertiesportal

# Optionally, you can specify the server port
# server.port=8081

# MongoDB connection settings
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=propertiesDB
